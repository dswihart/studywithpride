const{createClient}=require("@supabase/supabase-js");require("dotenv").config({path:".env.local"});const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;const s=createClient(u,k);s.from("tasks").select("*").limit(5).then(({data,error})=>{if(error){console.log("ERROR:",error.message,error.code);}else{console.log("FOUND:",data?data.length:0);data&&data[0]&&console.log(JSON.stringify(data[0]));}});
